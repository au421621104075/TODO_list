# TODO_list
 This code creates a simple To-Do List app where users can add tasks via an input field and remove them by clicking an "X" button. The tasks are displayed with a clean, responsive design using HTML, CSS, and JavaScript.
